import { useReducer} from "react";
import buttonReducer from '../Reducers/buttonReducer';


function Red() {

    const [buttonDispacher] = userReducer(buttonReducer, 'green');
        return (
        <div className='button-shelf'>
            
    )
    
}